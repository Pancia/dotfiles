# Pending CLAUDE.md Updates

_Generated: 20260326-033459_
_Session: 87c29588-26d2-485b-99b3-ec6c37d1aafe_

Post-session CLAUDE.md review system added, including automatic haiku-based suggestions for documentation updates.  **Changes needed:**  Add new section to CLAUDE.md documenting the Claude Code session review workflow and `.cc/` directory structure. Insert after "## Key Patterns" section:  ---  **OLD TEXT (to replace):** ``` ### Fish Shell Gotchas Common pitfalls when writing Fish functions: ```  **NEW TEXT:** ``` ### Claude Code Session Review  After each interactive Claude Code session, a Haiku process automatically reviews the session transcript and suggests CLAUDE.md updates. Recommendations are written to `.cc/pending-updates-<timestamp>-<session-id>.md`.  **Viewing suggestions:** Changes appear in `chpwd` output when you cd into a project with pending updates (golden/orange highlight). Read the file to see specific edits, then delete it when reviewed.  **How it works:** 1. `bin/cc-session-summary` — Extracts review-optimized session summary (human messages, file paths, tool use) with 150k char budget 2. `fish/functions/cc-session-review.fish` — Sends summary + current CLAUDE.md to Haiku, writes suggestions to `.cc/pending-updates-*.md` 3. Triggered automatically after `claude` exits (non-interactive invocations skipped)  ### Claude Code Project Artifacts  Files in `.cc/` are Claude Code session artifacts: - `pending-updates-<ts>-<id>.md` — Suggested CLAUDE.md edits from session reviews - `PLAN-*.md` — Session plans and design documents  These are tracked in git via `!.cc` in `gitignore_global`.  ### Fish Shell Gotchas Common pitfalls when writing Fish functions: ```
