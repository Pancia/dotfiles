# Pending CLAUDE.md Updates

_Generated: 20260408-184337_
_Session: c85116e6-707a-4038-b9ad-20ae42c0cf52_

Added new `ziplog` LaunchAgent service that compresses monitor logs, archives them monthly, and backs up to cloud weekly (Thursday noon).

---

**Add this subsection after the "## LaunchAgent Services" section:**

```markdown
### Available Services

Services are LaunchAgent background tasks managed via `service` CLI. Current services:

| Service | Schedule | Purpose |
|---------|----------|---------|
| `ziplog` | Thursday 12:00 | Compress monitor logs >6 days old, archive by month, backup to `~/Cloud/_inbox/monitor/minimac/` |
| `disk-snapshot` | Daily 12:30 | Create disk usage snapshot to `~/.local/share/disk-snapshots/` |
| `bookmark-manager` | Daily 2:00 | Sync browser bookmarks |
| `sanctuary` | On demand | Rotate encrypted backup directories |
| `copyparty` | Startup | Media server for file sharing |
| And others... | | See `service list` for all installed services |

**Note:** `bin/ziplog` can also be run manually with `ziplog --yes` (skips confirmation prompt).
```
