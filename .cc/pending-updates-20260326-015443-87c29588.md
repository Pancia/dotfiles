# Pending CLAUDE.md Updates

_Generated: 20260326-015443_
_Session: 87c29588-26d2-485b-99b3-ec6c37d1aafe_

New Claude Code session review workflow added to auto-check CLAUDE.md after sessions.  **Edit to add after the "Testing" subsection:**  Old: ``` ### Testing **Always use `cmds test` to run tests** - never run pytest directly with python. The `cmds` script handles virtual environments and dependencies.  ```bash cmds test yt            # Run youtube-transcribe tests cmds test yt -v         # Verbose output cmds test yt --cov      # With coverage report cmds test yt -k "cache" # Run tests matching pattern ```  ## Primary Tools ```  New: ``` ### Testing **Always use `cmds test` to run tests** - never run pytest directly with python. The `cmds` script handles virtual environments and dependencies.  ```bash cmds test yt            # Run youtube-transcribe tests cmds test yt -v         # Verbose output cmds test yt --cov      # With coverage report cmds test yt -k "cache" # Run tests matching pattern ```  ### Session Review Claude Code sessions are automatically reviewed when they exit. The system checks if CLAUDE.md or other project documentation needs updates based on what changed during the session.  **How it works:** - After a Claude Code session ends, `my-claude-code-wrapper` triggers `cc-session-review` - The session transcript is analyzed; if files were modified, haiku checks if CLAUDE.md needs updating - Recommendations are saved to `.claude/pending-updates-<timestamp>-<session-id>.md`  **Output:** - If no updates needed: File is not created (silent success) - If updates suggested: Review `.claude/pending-updates-*.md` to see recommendations  **Manual usage:** ```bash cc-session-review ~/.claude/projects/-Users-anthony-dotfiles/SESSION_ID.jsonl ```  ## Primary Tools ```
