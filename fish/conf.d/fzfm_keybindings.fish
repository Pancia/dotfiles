# Vim-style leader key system for fzfm
# Press Ctrl+S, then a letter to trigger different fzfm modes
#
# The functions _fzfm_leader and _fzfm_show_help are defined in
# fish/functions/ directory, and the keybinding is set in
# fish/functions/fish_user_key_bindings.fish
