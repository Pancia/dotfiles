#!/usr/bin/env fish
echo "file: $argv[1]"
echo "line: $argv[2]"
set -l line (test -n "$argv[2]"; and echo $argv[2]; or echo 0)
nvr --servername /tmp/filament.socket --remote-send ":e $argv[1]<CR>" -c $line
osascript -e 'tell application "iTerm" to activate'
