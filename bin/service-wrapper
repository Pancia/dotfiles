#!/usr/bin/env bash
# Wrapper that timestamps service output
# With proper signal handling in services, we can use simple exec
cd "$1" && exec "${@:2}" 2>&1 | /opt/homebrew/bin/ts '[%Y-%m-%d %H:%M:%S]'
