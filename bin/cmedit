#!/usr/bin/env bash

uuid=$(cmus-remote --raw status \
    | ag 'file ' \
    | sed 's/file //' \
    | xargs basename \
    | sed 's/\..*$//')

echo "edit -f '.id' $uuid" > $ZSH_INIT_CMD_DIR/music
open -a iTerm `dirname $MUSIC_DB`
