#!/usr/bin/env python3
"""VCS which-key modal menu - single-key navigation for git/jj commands."""
import sys
import os
import tty
import termios
import subprocess

# Detect jj repo (check for .jj/ directory up the tree)
def _is_jj_repo():
    d = os.getcwd()
    while True:
        if os.path.isdir(os.path.join(d, '.jj')):
            return True
        parent = os.path.dirname(d)
        if parent == d:
            return False
        d = parent

IS_JJ = _is_jj_repo()

# Theme detection (macOS light/dark mode)
def _is_dark_mode():
    try:
        subprocess.run(['defaults', 'read', '-g', 'AppleInterfaceStyle'],
                       capture_output=True, check=True)
        return True
    except subprocess.CalledProcessError:
        return False

_dark = _is_dark_mode()

# ANSI colors
BOLD_CYAN = '\033[1;96m'
BOLD_WHITE = '\033[1;37m' if _dark else '\033[1;30m'
BOLD_YELLOW = '\033[1;93m'
YELLOW = '\033[33m'
DIM = '\033[2;37m' if _dark else '\033[2;30m'
RESET = '\033[0m'
CYAN = '\033[36m'
GREEN = '\033[32m'
RED = '\033[31m'
MAGENTA = '\033[35m'


def jj_status_line():
    """Display jj status line."""
    def run(cmd):
        try:
            return subprocess.run(cmd, capture_output=True, text=True, timeout=2).stdout.strip()
        except:
            return ''

    # Get current change and bookmark info
    change_id = run(['jj', 'log', '--no-graph', '-r', '@', '-T', 'change_id.short(8)'])
    if not change_id:
        return

    # Get bookmark (branch) if any
    bookmark = run(['jj', 'log', '--no-graph', '-r', '@', '-T', 'bookmarks'])
    desc = run(['jj', 'log', '--no-graph', '-r', '@', '-T', 'description.first_line()'])

    # Check for changes
    status = run(['jj', 'status'])
    has_changes = 'Working copy changes:' in status
    has_conflict = 'conflict' in status.lower()

    if has_conflict:
        bg = '\033[41m'  # Red
    elif has_changes:
        bg = '\033[43m'  # Yellow
    else:
        bg = '\033[42m'  # Green
    black = '\033[30m'

    parts = [f'jj:{change_id}']
    if bookmark:
        parts.append(bookmark)
    if desc:
        parts.append(desc[:40])

    print(f"{bg}{black} {' '.join(parts)} {RESET}")


def git_status_line():
    """Display git status line matching Tide prompt format."""
    if IS_JJ:
        return jj_status_line()

    def run(cmd):
        try:
            return subprocess.run(cmd, capture_output=True, text=True, timeout=2).stdout.strip()
        except:
            return ''

    # Branch name (or tag/SHA if detached)
    branch = run(['git', 'branch', '--show-current'])
    if not branch:
        # Detached HEAD - try tag, then short SHA
        branch = run(['git', 'describe', '--tags', '--exact-match', 'HEAD'])
        if branch:
            branch = '#' + branch
        else:
            branch = '@' + run(['git', 'rev-parse', '--short', 'HEAD'])
    if not branch or branch in ('@', '#'):
        return  # Not in a git repo

    # Status counts from porcelain
    status = run(['git', 'status', '--porcelain'])
    staged = dirty = untracked = conflicted = 0
    for line in status.splitlines():
        if line.startswith('UU'):
            conflicted += 1
        elif line[0] in 'ADMR':
            staged += 1
        if len(line) > 1 and line[1] in 'ADMR':
            dirty += 1
        if line.startswith('??'):
            untracked += 1

    # Stash count
    stash = len(run(['git', 'stash', 'list']).splitlines()) if run(['git', 'stash', 'list']) else 0

    # Ahead/behind upstream
    ahead = behind = 0
    upstream = run(['git', 'rev-list', '--count', '--left-right', '@{upstream}...HEAD'])
    if upstream and '\t' in upstream:
        behind, ahead = upstream.split('\t')
        behind, ahead = int(behind), int(ahead)

    # Choose background color like Tide: green=clean, yellow=unstable, red=urgent
    if conflicted:
        bg = '\033[41m'  # Red background
    elif staged or dirty or untracked:
        bg = '\033[43m'  # Yellow background
    else:
        bg = '\033[42m'  # Green background
    black = '\033[30m'  # Black text for contrast

    # Build status line - only show non-zero values
    parts = [branch]
    if behind:
        parts.append(f"⇣{behind}")
    if ahead:
        parts.append(f"⇡{ahead}")
    if stash:
        parts.append(f"*{stash}")
    if conflicted:
        parts.append(f"~{conflicted}")
    if staged:
        parts.append(f"+{staged}")
    if dirty:
        parts.append(f"!{dirty}")
    if untracked:
        parts.append(f"?{untracked}")

    print(f"{bg}{black} {' '.join(parts)} {RESET}")

# VCS hooks support - runs ./vcs-hooks/<hook> from repo root if present
_COMMIT_PATTERNS = ('jj commit', 'jj squash', 'ai_jj_commit', 'git commit', 'ai_git_commit')

def _repo_root():
    """Find the repo root (containing .jj or .git)."""
    d = os.getcwd()
    while True:
        if os.path.isdir(os.path.join(d, '.jj')) or os.path.isdir(os.path.join(d, '.git')):
            return d
        parent = os.path.dirname(d)
        if parent == d:
            return None
        d = parent

def _run_vcs_hook(hook_name):
    """Run a vcs-hooks script if it exists."""
    root = _repo_root()
    if not root:
        return
    hook = os.path.join(root, 'vcs-hooks', hook_name)
    if os.path.isfile(hook) and os.access(hook, os.X_OK):
        print(f"{DIM}{'=' * 40}")
        print(f" vcs-hooks/{hook_name}")
        print(f"{'=' * 40}{RESET}")
        print()
        subprocess.run(hook, cwd=root)

def _chain_has_commit(command):
    """Check if a chain contains a commit-like command."""
    if isinstance(command, Chain):
        return any(any(p in cmd for p in _COMMIT_PATTERNS) for cmd in command)
    if isinstance(command, str):
        return any(p in command for p in _COMMIT_PATTERNS)
    return False


# Enhanced status: mirrors the `gs` fish function (diff --stat + separator + status)
_STATUS_SEP = '(set_color brblack)(string repeat -n 60 -)(set_color normal)'
GIT_STATUS = f'git diff --stat; and echo {_STATUS_SEP}; and git status'
JJ_STATUS = f'jj diff --stat; and echo {_STATUS_SEP}; and jj status'


# Chain: run multiple commands sequentially with headers between them
class Chain(tuple):
    """A sequence of shell commands to run with visual headers."""
    pass

def chain(*cmds):
    """Create a chain of commands that display with headers."""
    return Chain(cmds)


# Menu structure - nested dicts are submenus, tuples are (description, command, stay_in_submenu)
# stay_in_submenu=True stays in current submenu, False goes back to root
GIT_COMMANDS = {
    '_desc': 'Git',
    'a': {
        '_desc': 'Add',
        'f': ('Add file (fzf)', chain('printf \'%s\\n\' (git ls-files -m -o --exclude-standard) (git ls-files -m -o --exclude-standard | xargs -n1 dirname 2>/dev/null) | sort -u | grep -v \'^\\.\\$\' | fzf -m --bind "space:toggle+down" --bind "enter:select+accept" --header "SPACE=toggle  ENTER=accept" | xargs git add', GIT_STATUS), True),
        '.': ('Add this directory', chain('git add .', GIT_STATUS), True),
        'a': ('Add all', chain('git add --all', GIT_STATUS)),
        'p': ('Add interactively', chain('git add --patch', GIT_STATUS)),
    },
    'b': ('Blame (fzf)', 'git ls-files | fzf | xargs git blame'),
    'c': {
        '_desc': 'Commit',
        'c': ('Commit', chain('git commit --verbose', GIT_STATUS)),
        'a': ('Commit all', chain('git commit --verbose --all', GIT_STATUS)),
        'm': ('Amend', chain('git commit --amend --verbose', GIT_STATUS)),
        'p': ('Commit interactively', chain('git commit --patch', GIT_STATUS)),
        'i': ('Commit (AI message)', chain('ai_git_commit', GIT_STATUS)),
        'I': ('Add all + AI commit', chain('git add --all', 'ai_git_commit', GIT_STATUS)),
    },
    'd': {
        '_desc': 'Diff',
        'd': ('Diff', 'git diff --color-words', True),
        's': ('Diff staged', 'git diff --staged --color-words', True),
        'f': ('Diff file (fzf)', 'printf \'%s\\n\' (git ls-files -m) (git ls-files -m | xargs -n1 dirname 2>/dev/null) | sort -u | grep -v \'^\\.\\$\' | fzf -m --bind "space:toggle+down" --bind "enter:select+accept" --header "SPACE=toggle  ENTER=accept" | xargs git diff --color-words', True),
    },
    'f': {
        '_desc': 'Fetch/Pull',
        'f': ('Fetch all', chain('git fetch --all --prune', GIT_STATUS)),
        'l': ('Pull', chain('git pull', GIT_STATUS)),
    },
    'h': {
        '_desc': 'Show',
        'h': ('Show (last commit)', 'git show --color-words'),
        'f': ('Show commit (fzf)', 'git log --oneline --all | fzf --preview "git show --color-words {1}" | awk \'{print $1}\' | xargs git show --color-words'),
    },
    'l': ('Log graph', 'git log --graph --all --decorate --abbrev-commit'),
    'p': {
        '_desc': 'Push',
        'p': ('Push', chain('git push', GIT_STATUS)),
        'f': ('Push force++', chain('read -l -P "Force push? [y/N] " confirm; and test "$confirm" = y; and git push --force-with-lease --force-if-includes', GIT_STATUS)),
        },
    'u': ('Pull', chain('git pull', GIT_STATUS)),
    'r': {
        '_desc': 'Restore',
        's': ('Unstage (fzf)', chain('printf \'%s\\n\' (git diff --staged --name-only) (git diff --staged --name-only | xargs -n1 dirname 2>/dev/null) | sort -u | grep -v \'^\\.\\$\' | fzf -m --bind "space:toggle+down" --bind "enter:select+accept" --header "SPACE=toggle  ENTER=accept" | xargs git restore --staged', GIT_STATUS), True),
        'r': ('Restore file (fzf)', chain('set -l files (printf \'%s\\n\' (git diff --name-only) (git diff --name-only | xargs -n1 dirname 2>/dev/null) | sort -u | grep -v \'^\\.\\$\' | fzf -m --bind "space:toggle+down" --bind "enter:select+accept" --header "SPACE=toggle  ENTER=accept"); and read -l -P "Discard changes? [y/N] " confirm; and test "$confirm" = y; and printf \'%s\\n\' $files | xargs git restore', GIT_STATUS), True),
    },
    's': ('Status', GIT_STATUS),
    't': {
        '_desc': 'Stash',
        't': ('Stash', chain('git stash', GIT_STATUS)),
        's': ('Stash save (named)', chain('git stash save', GIT_STATUS)),
        'p': ('Stash pop', chain('git stash pop', GIT_STATUS)),
        'l': ('Stash list', 'git stash list', True),
    },
    'w': ('Switch branch (fzf)', chain('git branch -a | fzf | xargs git checkout', GIT_STATUS)),
    '=': ('Edit this menu', ['nvim', __file__, '-c', '/^GIT_COMMANDS', '-c', 'normal zt'], False, True),  # go to root, reload
}

JJ_COMMANDS = {
    '_desc': 'Jujutsu',
    'b': {
        '_desc': 'Bookmark',
        'c': ('Create', 'read -l -P "Bookmark name: " name; and jj bookmark create -r @ $name'),
        'd': ('Delete', 'set -l bm (jj bookmark list | fzf | awk \'{print $1}\'); and read -l -P "Delete bookmark $bm? [y/N] " confirm; and test "$confirm" = y; and jj bookmark delete $bm'),
        'l': ('List', 'jj bookmark list', True),
        's': ('Set to @', 'jj bookmark list | fzf | awk \'{print $1}\' | xargs -I{} jj bookmark set {} -r @'),
    },
    'c': {
        '_desc': 'Commit',
        'c': ('Commit (describe + new)', chain('jj commit', JJ_STATUS)),
        'C': ('Commit + advance master + push', chain('jj commit', 'jj bookmark set master --allow-backwards -r @-', 'jj git push', JJ_STATUS)),
        'h': ('Commit interactive (pick hunks)', chain('jj commit --interactive', JJ_STATUS)),
        'd': ('Describe @', chain('jj describe', JJ_STATUS)),
        'p': ('Describe @-', chain('jj describe @-', JJ_STATUS)),
        'i': ('Commit (AI message)', chain('ai_jj_commit', JJ_STATUS)),
        'I': ('AI commit + advance master + push', chain('ai_jj_commit', 'jj bookmark set master --allow-backwards -r @-', 'jj git push', JJ_STATUS)),
    },
    'd': {
        '_desc': 'Diff',
        'd': ('Diff (@)', 'jj diff', True),
        'r': ('Diff revision', 'jj log --no-graph -T \'change_id.short(8) ++ " " ++ description.first_line() ++ "\\n"\' | fzf | awk \'{print $1}\' | xargs jj diff -r', True),
        'f': ('Diff from parent', 'jj diff --from @-', True),
    },
    'e': ('Edit revision (fzf)', chain('jj log --no-graph -T \'change_id.short(8) ++ " " ++ description.first_line() ++ "\\n"\' | fzf | awk \'{print $1}\' | xargs jj edit', JJ_STATUS)),
    'f': ('Fetch', chain('jj git fetch', JJ_STATUS)),
    'h': {
        '_desc': 'Show',
        'h': ('Show @', 'jj show', True),
        'p': ('Show @-', 'jj show @-', True),
        'f': ('Show revision (fzf)', 'jj log --no-graph -T \'change_id.short(8) ++ " " ++ description.first_line() ++ "\\n"\' | fzf | awk \'{print $1}\' | xargs jj show', True),
    },
    'l': ('Log', 'jj log'),
    'n': ('New (empty change)', chain('jj new', JJ_STATUS)),
    'p': {
        '_desc': 'Push',
        'p': ('Push', chain('jj git push', JJ_STATUS)),
        'a': ('Push all bookmarks', chain('jj git push --all', JJ_STATUS)),
        'm': ('Advance master to @- + push', chain('jj bookmark set master --allow-backwards -r @-', 'jj git push', JJ_STATUS)),
        'M': ('Advance master to @- (no push)', chain('jj bookmark set master --allow-backwards -r @-', JJ_STATUS)),
    },
    'q': {
        '_desc': 'Squash',
        'q': ('Squash into parent', chain('jj squash', JJ_STATUS)),
        'i': ('Squash interactive', chain('jj squash --interactive', JJ_STATUS)),
    },
    'r': {
        '_desc': 'Rebase/Restore',
        'r': ('Restore file (fzf)', chain('jj diff --name-only | fzf -m --bind "space:toggle+down" --bind "enter:select+accept" --header "SPACE=toggle  ENTER=accept" | xargs -I{} jj restore --from @- {}', JJ_STATUS), True),
        'b': ('Rebase onto (fzf)', 'jj log --no-graph -T \'change_id.short(8) ++ " " ++ description.first_line() ++ "\\n"\' | fzf | awk \'{print $1}\' | xargs -I{} jj rebase -r @ -d {}'),
    },
    's': ('Status', JJ_STATUS),
    'S': {
        '_desc': 'Split',
        'S': ('Split @ (interactive)', chain('jj split', JJ_STATUS)),
        'p': ('Split @ parallel (siblings)', chain('jj split -p', JJ_STATUS)),
        'r': ('Split revision (fzf)', chain('jj log --no-graph -T \'change_id.short(8) ++ " " ++ description.first_line() ++ "\\n"\' | fzf | awk \'{print $1}\' | xargs -I{} jj split -r {}', JJ_STATUS)),
    },
    'u': ('Undo', chain('read -l -P "Undo last jj operation? [y/N] " confirm; and test "$confirm" = y; and jj undo', JJ_STATUS)),
    'x': {
        '_desc': 'Abandon "all hope ye..."',
        'x': ('Abandon @ "who enter here"', chain('read -l -P "Abandon current change? [y/N] " confirm; and test "$confirm" = y; and jj abandon @', JJ_STATUS))
    },
    '?': ('Quick reference', '_jj_help', True),
    '=': ('Edit this menu', ['nvim', __file__, '-c', '/^JJ_COMMANDS', '-c', 'normal zt'], False, True),
}

COMMANDS = JJ_COMMANDS if IS_JJ else GIT_COMMANDS

JJ_HELP = f"""{BOLD_WHITE}Jujutsu Quick Reference{RESET}

{BOLD_YELLOW}Key concepts:{RESET}
  {BOLD_WHITE}No staging area{RESET} — every file save is automatically part of @ (current change)
  {BOLD_WHITE}@{RESET}  = your current change (working copy)
  {BOLD_WHITE}@-{RESET} = parent of current change

{BOLD_YELLOW}Core workflow:{RESET}
  {CYAN}edit{RESET}      Switch working copy to any mutable change
  {CYAN}new{RESET}       Create empty change on top of @ (start fresh)
  {CYAN}describe{RESET}  Set/edit the message on @ (keep working in it)
  {CYAN}commit{RESET}    Describe @ + create new change on top (you're "done")

{BOLD_YELLOW}Rewriting history:{RESET}
  {CYAN}squash{RESET}    Fold @ into its parent (or pick hunks with --interactive)
  {CYAN}split{RESET}     Break @ into two changes (interactive picker)
  {CYAN}rebase{RESET}    Move @ onto a different parent
  {CYAN}abandon{RESET}   Delete @ and discard its changes

{BOLD_YELLOW}Safety net:{RESET}
  {CYAN}undo{RESET}      Reverse the last jj operation (always safe to experiment)

{BOLD_YELLOW}Git interop:{RESET}
  {CYAN}jj git fetch{RESET}   = git fetch
  {CYAN}jj git push{RESET}    = git push
  {CYAN}bookmark{RESET}       = branches (just pointers to changes)

{BOLD_YELLOW}Revsets (revision selection):{RESET}
  {DIM}@{RESET}          current change       {DIM}@-{RESET}         parent
  {DIM}main{RESET}       bookmark             {DIM}@--{RESET}        grandparent
  {DIM}x | y{RESET}      union                {DIM}x & y{RESET}      intersection
  {DIM}::x{RESET}        ancestors            {DIM}x::{RESET}        descendants
"""


def getch():
    """Read a single character without echo."""
    fd = sys.stdin.fileno()
    if not os.isatty(fd):
        sys.exit(0)
    old_settings = termios.tcgetattr(fd)
    try:
        tty.setraw(fd)
        return sys.stdin.read(1)
    finally:
        termios.tcsetattr(fd, termios.TCSADRAIN, old_settings)


def print_menu(commands, path=''):
    """Display the menu with available commands."""
    entries = []

    for key, content in commands.items():
        if key.startswith('_'):
            continue
        if isinstance(content, tuple):
            desc = content[0]
            formatted = f"{BOLD_CYAN}[{key}]{RESET} {BOLD_WHITE}{desc}{RESET}"
            plain_len = len(f"[{key}] {desc}")
            entries.append((key, formatted, plain_len))
        else:
            desc = content.get('_desc', key)
            formatted = f"{BOLD_YELLOW}[{key}]{RESET} {YELLOW}→ {desc}{RESET}"
            plain_len = len(f"[{key}] → {desc}")
            entries.append((key, formatted, plain_len))

    entries.sort(key=lambda x: x[0].lower())

    if not entries:
        return

    # Get terminal width
    try:
        term_width = os.get_terminal_size().columns
    except OSError:
        term_width = 80

    # Calculate columns
    max_width = max(e[2] for e in entries) + 2
    num_cols = max(1, term_width // max_width)
    num_rows = (len(entries) + num_cols - 1) // num_cols

    # Print separator and header
    print(f"{DIM}{'─' * 40}{RESET}")
    if path:
        menu_desc = commands.get('_desc', '')
        print(f"{DIM}{path}{RESET}{BOLD_WHITE}{menu_desc}{RESET}")
        print()

    # Print in columns
    for row in range(num_rows):
        line_parts = []
        for col in range(num_cols):
            idx = col * num_rows + row
            if idx < len(entries):
                _, formatted, plain_len = entries[idx]
                padding = ' ' * (max_width - plain_len)
                line_parts.append(formatted + padding)
        print(''.join(line_parts).rstrip())

    if path:
        print(f"{DIM}[:] Search  <DEL> Go back  <ESC>/q Exit{RESET}")
    else:
        print(f"{DIM}[:] Search  <ESC>/q Exit{RESET}")
    print(f"{DIM}{'─' * 40}{RESET}")
    git_status_line()


def execute_command(command, label=None):
    """Execute a git/jj command."""
    if label:
        print(f"\n{BOLD_CYAN}{label}{RESET}\n", flush=True)
    if command == '_jj_help':
        print(JJ_HELP)
        return
    run_post_commit = _chain_has_commit(command)
    if isinstance(command, Chain):
        for i, cmd in enumerate(command):
            if i > 0:
                print()
            print(f"{DIM}{'=' * 40}")
            print(f" {cmd}")
            print(f"{'=' * 40}{RESET}")
            print()
            result = subprocess.run(cmd, shell=True, executable='/opt/homebrew/bin/fish')
            if result.returncode != 0:
                run_post_commit = False
                break
        if run_post_commit:
            _run_vcs_hook('post-commit')
        return
    if isinstance(command, list):
        # Direct execution without shell
        print(f"{DIM}$ {' '.join(command)}{RESET}\n", flush=True)
        subprocess.run(command)
    else:
        # Handle % as current file placeholder
        if '%' in command:
            print(f"{YELLOW}Note: '%' placeholder not available in shell (use in editor){RESET}")
            command = command.replace(' %', '')
        print(f"{DIM}$ {command}{RESET}\n", flush=True)
        subprocess.run(command, shell=True, executable='/opt/homebrew/bin/fish')


def reload_commands():
    """Reload COMMANDS from this file."""
    global COMMANDS, GIT_COMMANDS, JJ_COMMANDS
    with open(__file__, 'r') as f:
        source = f.read()
    exec(compile(source, __file__, 'exec'), globals())
    COMMANDS = JJ_COMMANDS if IS_JJ else GIT_COMMANDS
    return COMMANDS


def flatten_commands(commands, path=""):
    """Recursively flatten menu structure into searchable entries."""
    entries = []
    for key, content in commands.items():
        if key.startswith('_'):
            continue
        if isinstance(content, tuple):
            desc = content[0]
            cmd = content[1]
            display = f"{path}{desc}" if path else desc
            entries.append((display, content))
        else:
            submenu_desc = content.get('_desc', key)
            new_path = f"{path}{submenu_desc} → " if path else f"{submenu_desc} → "
            entries.extend(flatten_commands(content, new_path))
    return entries


def fzf_search_mode():
    """Launch fzf to search through all commands. Returns the command tuple or None."""
    entries = flatten_commands(COMMANDS)
    if not entries:
        return None

    fzf_input = "\n".join(entry[0] for entry in entries)

    try:
        result = subprocess.run(
            ['fzf', '--prompt=: ', '--height=40%', '--reverse'],
            input=fzf_input,
            capture_output=True,
            text=True
        )
        if result.returncode == 0 and result.stdout.strip():
            selected = result.stdout.strip()
            for display, content in entries:
                if display == selected:
                    return content, display
    except FileNotFoundError:
        print(f"{YELLOW}fzf not found{RESET}")
    return None


def list_commands(commands=None, prefix='', out=None):
    """List all commands with their key paths."""
    if commands is None:
        commands = COMMANDS
    if out is None:
        out = []
    for key, content in sorted(commands.items()):
        if key.startswith('_'):
            continue
        path = prefix + key
        if isinstance(content, tuple):
            desc = content[0]
            cmd = content[1]
            if isinstance(cmd, Chain):
                cmd_str = ' && '.join(cmd)
            elif isinstance(cmd, list):
                cmd_str = ' '.join(cmd)
            else:
                cmd_str = str(cmd)
            out.append((path, desc, cmd_str))
        else:
            submenu_desc = content.get('_desc', key)
            list_commands(content, path, out)
    return out


def run_keys(keys):
    """Navigate the menu by key sequence and execute the resulting command."""
    current = COMMANDS
    for i, key in enumerate(keys):
        if key not in current:
            used = keys[:i] if i > 0 else ''
            print(f"{RED}Unknown key '{key}' at position {i+1}{RESET}", file=sys.stderr)
            if isinstance(current, dict):
                avail = [k for k in current if not k.startswith('_')]
                print(f"{DIM}Available: {' '.join(sorted(avail))}{RESET}", file=sys.stderr)
            return 1
        action = current[key]
        if isinstance(action, tuple):
            # Found a command — execute it
            if i < len(keys) - 1:
                print(f"{YELLOW}Warning: ignoring extra keys after '{keys[:i+1]}'{RESET}", file=sys.stderr)
            execute_command(action[1], label=action[0])
            return 0
        else:
            # Enter submenu
            current = action
    # Ended on a submenu, not a command
    print(f"{RED}'{keys}' is a submenu, not a command. Add another key:{RESET}", file=sys.stderr)
    for k, v in sorted(current.items()):
        if k.startswith('_'):
            continue
        if isinstance(v, tuple):
            print(f"  {BOLD_CYAN}{keys}{k}{RESET}  {v[0]}", file=sys.stderr)
        else:
            print(f"  {BOLD_YELLOW}{keys}{k}{RESET}  → {v.get('_desc', k)}", file=sys.stderr)
    return 1


def main_interactive():
    current = COMMANDS
    history = []
    path = ''

    # Show status on startup
    subprocess.run(JJ_STATUS if IS_JJ else GIT_STATUS,
                   shell=True, executable='/opt/homebrew/bin/fish')
    print()

    while True:
        print_menu(current, path)
        key = getch()

        if key in ('\x1b', '\x03', '\x04', 'q'):  # ESC, Ctrl-C, Ctrl-D, q
            break
        elif key in ('\x7f', '\x08'):  # DEL, Backspace
            if history:
                current, path = history.pop()
            else:
                break
        elif key == ':':
            # fzf search mode
            result = fzf_search_mode()
            if result:
                content, display = result
                cmd = content[1]
                stay_in_submenu = content[2] if len(content) > 2 else False
                execute_command(cmd, label=display)
                if not stay_in_submenu:
                    current = COMMANDS
                    history = []
                    path = ''
        elif key in current:
            action = current[key]
            if isinstance(action, tuple):
                # Execute command - tuple is (desc, cmd, stay_in_submenu, reload)
                desc = action[0]
                cmd = action[1]
                stay_in_submenu = action[2] if len(action) > 2 else False
                should_reload = action[3] if len(action) > 3 else False
                menu_desc = current.get('_desc', '')
                label = f"{path}{menu_desc} → {desc}" if path or menu_desc else desc
                execute_command(cmd, label=label)
                if should_reload:
                    current = reload_commands()
                    history = []
                    path = ''
                elif not stay_in_submenu:
                    # Go back to root
                    current = COMMANDS
                    history = []
                    path = ''
            else:
                # Enter submenu
                history.append((current, path))
                path = f"{path}{current.get('_desc', '')} → "
                current = action
        else:
            print(f"{YELLOW}Unknown key: {repr(key)}{RESET}\n")


def main():
    if len(sys.argv) < 2:
        main_interactive()
        return

    subcmd = sys.argv[1]

    if subcmd in ('ls', 'help'):
        entries = list_commands()
        # Determine column widths
        max_keys = max(len(e[0]) for e in entries)
        max_desc = max(len(e[1]) for e in entries)
        for path, desc, cmd in entries:
            print(f"{BOLD_CYAN}{path:<{max_keys}}{RESET}  {BOLD_WHITE}{desc:<{max_desc}}{RESET}  {DIM}{cmd}{RESET}")

    elif subcmd == 'run':
        if len(sys.argv) < 3:
            result = fzf_search_mode()
            if not result:
                sys.exit(1)
            content, display = result
            cmd = content[1]
            execute_command(cmd, label=display)
            sys.exit(0)
        keys = sys.argv[2]
        sys.exit(run_keys(keys))

    else:
        print(f"{RED}Unknown subcommand: {subcmd}{RESET}", file=sys.stderr)
        print(f"{DIM}Usage: g [ls|help | run <keys>]{RESET}", file=sys.stderr)
        sys.exit(1)


if __name__ == '__main__':
    main()
